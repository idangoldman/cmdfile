#!/usr/bin/env python3

import os
import pathlib
import sys

if __name__ == '__main__':
  package_path = os.path.join(
    pathlib.Path(__file__).parent.parent.absolute(), 'src'
  )

  sys.path.append(package_path)

  from core import run

  sys.exit(run())
